services:
  - type: web
    name: medcore
    runtime: docker
    healthCheckPath: /api/health
    disk:
      name: medcore-data
      mountPath: /var/data
      sizeGB: 1
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: ADMIN_EMAIL
        value: ma7moud01030382018@gmail.com
      - key: ADMIN_PASSWORD
        sync: false
      - key: CLIENT_ORIGIN
        sync: false
      - key: DATABASE_PATH
        value: /var/data/medcore.db
