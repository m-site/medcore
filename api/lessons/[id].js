import { cert, getApps, initializeApp } from 'firebase-admin/app';
import { getFirestore } from 'firebase-admin/firestore';
function credentials() { const raw = process.env.FIREBASE_SERVICE_ACCOUNT_JSON || ''; const value = raw.startsWith('ey') ? Buffer.from(raw, 'base64').toString('utf8') : raw; try { return JSON.parse(value); } catch { return JSON.parse(value.replace(/("private_key"\s*:\s*")([\s\S]*?)(",\s*"client_email")/, (_, a, key, b) => a + key.replace(/\r?\n/g, '\\n') + b)); } }
export default async function handler(request, response) { try { const app = getApps()[0] || initializeApp({ credential: cert(credentials()) }); const doc = await getFirestore(app).collection('lessons').doc(request.query.id).get(); if (!doc.exists || !doc.data().published) return response.status(404).json({ error: 'الدرس غير موجود.' }); const { questions = [], ...lesson } = doc.data(); response.json({ ...lesson, questions: questions.map(({ answerIndex, explanation, ...question }) => question) }); } catch (error) { response.status(500).json({ error: error.message }); } }
